version: '3.8'

services:
  # ── Tenebra Web (Nginx + SPA) ────────────────────────────────────────────
  web:
    build:
      context: ./tenebra-web
      dockerfile: Dockerfile
    container_name: tenebra-web
    restart: unless-stopped
    ports:
      - '80:8080'
    depends_on:
      server:
        condition: service_healthy
    healthcheck:
      test: ['CMD', 'wget', '--no-verbose', '--tries=1', '--spider', 'http://localhost:8080']
      interval: 30s
      timeout: 10s
      start_period: 5s
      retries: 3

  # ── Tenebra Chat Server (API + Socket.io) ────────────────────────────────
  server:
    build:
      context: ./tenebra-server
      dockerfile: Dockerfile
    container_name: tenebra-server
    restart: unless-stopped
    environment:
      NODE_ENV: ${NODE_ENV:-production}
      PORT: ${PORT:-3000}
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: ${DB_NAME:-tenebra}
      DB_USER: ${DB_USER:-tenebra_user}
      DB_PASSWORD: ${DB_PASSWORD}
      MINIO_ENDPOINT: minio
      MINIO_PORT: 9000
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:-minioadmin}
      MINIO_BUCKET: ${MINIO_BUCKET:-tenebra-files}
      MINIO_USE_SSL: 'false'
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-7d}
      LOW_KEY_THRESHOLD: ${LOW_KEY_THRESHOLD:-20}
    expose:
      - '3000'
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
    healthcheck:
      test: ['CMD', 'wget', '--no-verbose', '--tries=1', '--spider', 'http://localhost:3000/health']
      interval: 30s
      timeout: 10s
      start_period: 10s
      retries: 3

  # ── PostgreSQL ───────────────────────────────────────────────────────────
  postgres:
    image: postgres:16-alpine
    container_name: tenebra-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_NAME:-tenebra}
      POSTGRES_USER: ${DB_USER:-tenebra_user}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - '5433:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U tenebra_user -d tenebra']
      interval: 10s
      timeout: 5s
      retries: 5

  # ── MinIO (S3-compatible object storage) ─────────────────────────────────
  minio:
    image: minio/minio:latest
    container_name: tenebra-minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY:-minioadmin}
    ports:
      - '9000:9000'
      - '9001:9001'
    volumes:
      - minio_data:/data
    healthcheck:
      test: ['CMD', 'mc', 'ready', 'local']
      interval: 30s
      timeout: 20s
      retries: 3

  # ── MinIO init (create bucket on first run) ──────────────────────────────
  minio-init:
    image: minio/mc:latest
    container_name: tenebra-minio-init
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      mc alias set myminio http://minio:9000 ${MINIO_ACCESS_KEY:-minioadmin} ${MINIO_SECRET_KEY:-minioadmin};
      mc mb myminio/${MINIO_BUCKET:-tenebra-files} --ignore-existing;
      mc anonymous set download myminio/${MINIO_BUCKET:-tenebra-files};
      exit 0;
      "

volumes:
  postgres_data:
  minio_data:
